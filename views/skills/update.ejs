<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update</title>
</head>
<body>
    <h1>Update skill</h1>
    <form action="/skills/<%= t.id %>?_method=PUT" class="delete-form" method="POST">
        <input type="text" name="skill" value="<%= skill.skill %>">
        <input name="done" type="checkbox" <% skill.done ? 'checked' : '' %>>
        <button type="submit">Update skill</button>
    </form>
</body>
</html>